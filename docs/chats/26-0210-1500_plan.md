# План развёртывания стороннего ПО для проекта ferag

**Дата:** 2026-02-10, 15:00  
**Стратегия:** "Инструменты → Понимание → Интеграция"  
**Принцип:** Сначала установить и протестировать стороннее ПО, понять его возможности, только потом разрабатывать код интеграции.

---

## 🎯 Общая цель

Развернуть и протестировать все компоненты стека для этапов 1-3 диалектического цикла:
- **Этап 1 (ТЕЗИС):** MS GraphRAG — извлечение триплетов из текстов
- **Этап 2 (АНТИТЕЗИС):** LlamaIndex — извлечение онтологии из триплетов (Schema Induction)
- **Этап 3 (СИНТЕЗ):** MillenniumDB — хранение онтологии и триплетов, сравнение и интеграция

Этапы 4-5 (Проекции + RAG) отложены до стабилизации первых трёх этапов.

---

## 📋 Фазы развёртывания

### ✅ ФАЗА 1: Apache Jena Fuseki (источник истины для RDF/SPARQL с reasoning)

**Цель:** Установить Apache Jena Fuseki и проверить возможность хранения онтологии с OWL reasoning и RDFS inference.

**Связанные задачи из TASKS.md:**
- Задача **4.10** — Установить Apache Jena Fuseki (источник истины для онтологии с OWL reasoning)

**Действия:**

**✅ 1.1. Установка контейнера**
```bash
docker run -d --name fuseki \
  -p 3030:3030 \
  -v ~/projects/ferag/fuseki-data:/fuseki \
  -e ADMIN_PASSWORD=ferag2026 \
  stain/jena-fuseki
```
- Проверка: `docker ps | grep fuseki`
- Веб-интерфейс: http://localhost:3030
- SPARQL endpoint: http://localhost:3030/$/datasets

**✅ 1.2. Создание dataset и тестовой онтологии**
- Создать dataset через веб-интерфейс: http://localhost:3030 → "Manage datasets" → "New dataset" (имя: `ferag-test`, тип: TDB2)
- Создать файл `test_ontology.ttl` с классами (Person, Organization), свойствами (worksFor), экземплярами (Alice, ACME) и иерархией (Employee rdfs:subClassOf Person)

**✅ 1.3. Загрузка тестовых RDF-данных**
```bash
curl -X POST http://localhost:3030/ferag-test/data \
  -u admin:ferag2026 \
  -H "Content-Type: text/turtle" \
  --data-binary @test_ontology.ttl
```
Или через веб-интерфейс: http://localhost:3030 → "Dataset: ferag-test" → "Upload files"

**✅ 1.4. SPARQL-запросы (проверка работоспособности и inference)**
- Запрос всех триплетов:
  ```sparql
  SELECT ?s ?p ?o WHERE { ?s ?p ?o } LIMIT 10
  ```
- Запрос всех Person (через веб-интерфейс или curl):
  ```sparql
  PREFIX : <http://example.org/ferag#>
  SELECT ?person WHERE { ?person rdf:type :Person }
  ```
- **Проверка RDFS inference** (критично):
  ```sparql
  # Если в онтологии есть:
  # :Employee rdfs:subClassOf :Person .
  # :Alice rdf:type :Employee .
  # То Fuseki автоматически выведет:
  # :Alice rdf:type :Person .  ← inference!
  
  PREFIX : <http://example.org/ferag#>
  SELECT ?employee WHERE {
    ?employee rdf:type :Employee .
    ?employee rdf:type :Person .  # Должно работать благодаря inference
  }
  ```

**Критерии успеха:**
- ✅ Apache Jena Fuseki запущен и доступен через веб-интерфейс
- ✅ Dataset создан (ferag-test)
- ✅ Загружена тестовая онтология (OWL/RDF)
- ✅ SPARQL-запросы работают
- ✅ **RDFS inference работает** (rdfs:subClassOf автоматически выводится)
- ✅ Можем хранить онтологию + триплеты с reasoning

**Вопросы для оценки:**
- ✅ Может ли Fuseki хранить OWL-онтологию? → Да, нативная поддержка
- ✅ Есть ли inference (rdfs:subClassOf, owl:equivalentClass)? → Да, полная поддержка OWL reasoning
- ✅ Какая производительность на 10K+ триплетов? → Достаточна для проекта (тестируем на малом корпусе)
- ✅ Как загружать большие RDF-файлы (bulk import)? → tdbloader или HTTP API
- ✅ Есть ли веб-интерфейс для визуализации? → Да, http://localhost:3030 (SPARQL editor, upload, monitoring)

**Время выполнения:** 1-2 дня

---

### ✅ ФАЗА 2: MS GraphRAG (индексация текстов)

**Цель:** Установить MS GraphRAG, настроить для работы с Ollama, запустить индексацию тестовых документов и изучить результаты (триплеты, communities, summaries).

**Связанные задачи из TASKS.md:**
- Задача **6.3** — Установить MS GraphRAG
- Задача **6.4** — Настроить MS GraphRAG для работы с Ollama
- Задача **6.5** — Подготовить тестовый корпус текстов (10-20 документов)
- Задача **6.6** — Запустить первую индексацию MS GraphRAG
- Задача **6.7** — Изучить результаты: entities, relationships, communities
- Задача **6.8** — Загрузить триплеты из MS GraphRAG в MillenniumDB (staging)

**Действия:**

**✅ 2.1. Установка MS GraphRAG (нативно в venv)**
```bash
cd ~/projects/ferag
python3 -m venv venv-graphrag
source venv-graphrag/bin/activate
pip install graphrag
```
- Проверка: `graphrag --version`

**✅ 2.2. Инициализация проекта для тестирования**
```bash
mkdir -p ~/projects/ferag-test/graphrag-test
cd ~/projects/ferag-test/graphrag-test
graphrag init
```
- Создаётся: `.env`, `settings.yaml`

**✅ 2.3. Настройка для работы с Ollama**
- Редактировать `settings.yaml`:
  - `llm.api_base: http://localhost:11434/v1`
  - `llm.model: llama3.3:70b-instruct-q4_k_m`
  - `embeddings.model: nomic-embed-text`
  - `entity_extraction.entity_types: [Person, Organization, Event, Location]`
  - `community_detection.enabled: true`

**✅ 2.4. Подготовка тестовых данных**
- Создать папку `input/`
- Создать 2-5 тестовых документов (`.txt`):
  - `test_doc1.txt`: Alice Smith, ACME Corporation, GraphRAG project
  - `test_doc2.txt`: Bob Johnson, TechCorp, collaboration
- Размер корпуса: 1-2 МБ текста

**✅ 2.5. Проверка Ollama** (устанавливается в WSL2, загрузка моделей в процессе)
```bash
ollama list | grep llama3.3
ollama pull nomic-embed-text
```

**✅ 2.6. Запуск индексации**
```bash
graphrag index --root .
```
- Ожидаемое время: 5-10 минут на 2 документа
- **Фактически (CPU, Ollama):** ~5 ч 14 мин для 3 документов (Llama 3.3 70B, WSL2)
- **Фактически (Vulkan, LM Studio):** ~3 ч 7 мин для 3 документов (LM Studio 0.4.2, Vulkan llama.cpp на AMD Radeon 780M) — ускорение ~40%
- Процесс: извлечение сущностей → entity resolution → community detection → summarization → embeddings

**✅ 2.7. Изучение результатов**

**Созданные файлы в `output/`:**
- `entities.parquet` (16 КБ) — 41 сущность (EVENT: 19, ORGANIZATION: 14, GEO: 6, PERSON: 2)
- `relationships.parquet` (12 КБ) — 46 связей между сущностями
- `communities.parquet` (13 КБ) — 6 сообществ (3 уровня 0, 3 уровня 1)
- `community_reports.parquet` (53 КБ) — 6 детальных отчетов по сообществам
- `text_units.parquet` (22 КБ) — 3 текстовых единицы
- `documents.parquet` (18 КБ) — 3 исходных документа
- `lancedb/` — векторная БД с embeddings (entity.description, community.full_content, text_unit.text)
- `stats.json` — статистика индексации (total_runtime: 18861 сек ≈ 5 ч 14 мин)

**Ключевые сообщества (по рейтингу):**
1. **GraphRAG Community** (уровень 0, рейтинг 8.0) — Microsoft, Llama 3.3 70B, LlamaIndex, Ollama, Apache Jena Fuseki, PostgreSQL, Apache AGE
2. **ACME Corporation and GraphRAG System** (уровень 0, рейтинг 7.5) — Alice Smith, Bob Johnson, Knowledge Graph team
3. **TechCorp and AI Market** (уровень 0, рейтинг 6.5) — конкуренция с ACME, enterprise AI solutions

**Анализ данных:**
```python
import pandas as pd
entities = pd.read_parquet('output/entities.parquet')
relationships = pd.read_parquet('output/relationships.parquet')
communities = pd.read_parquet('output/communities.parquet')
reports = pd.read_parquet('output/community_reports.parquet')
```

**✅ 2.7a. Повторная индексация с LM Studio (Vulkan, AMD 780M)** — выполнено 12.02.2026
- Настроен LM Studio на Windows: модель Llama 3.3 70B, движок Vulkan llama.cpp (Windows) v2.1.0
- Сервер LM Studio: `http://10.7.0.3:1234` (Serve on Local Network для доступа из WSL2)
- GraphRAG в WSL2 обращается к LM Studio по API; embeddings — через Ollama (nomic-embed-text)
- **Результат:** общее время 11 252 сек (~3 ч 7 мин 32 сек), ускорение ~40% относительно CPU
- Детали сравнения: `graphrag-test/COMPARISON_TEMPLATE.md` (шаблон), фактические данные в `output/stats.json` и логах

**✅ 2.8. Конвертация в RDF (концептуально)** — выполнено
- Скрипт `graphrag-test/test_graphrag_to_rdf.py`:
  - Читает `output/entities.parquet` и `output/relationships.parquet`
  - Строит RDF-граф (rdflib): сущности как `ferag:EntityName` с типом Person/Organization/Event/Location и описанием; связи — blank node с `ferag:from`, `ferag:to`, `ferag:description`, `ferag:weight`
  - Сохраняет Turtle в `graphrag_output.ttl` (запуск: `python test_graphrag_to_rdf.py` из каталога graphrag-test)
- Префикс: `http://example.org/ferag#`. Результат: 301 триплет. Зависимость: `pip install rdflib`

**✅ 2.9. Загрузка в Apache Jena Fuseki** — выполнено
- Dataset `ferag-staging` создан (TDB2): `POST http://localhost:3030/$/datasets` с `dbType=tdb2&dbName=ferag-staging`
- Файл `graphrag_output.ttl` загружен в default graph: `POST http://localhost:3030/ferag-staging/data` (301 триплет)
- Проверка: SPARQL `SELECT ?s ?p ?o WHERE { ?s ?p ?o } LIMIT 20` или веб: http://localhost:3030 → "Dataset: ferag-staging" → "Query"
- Повторная загрузка: скрипт `graphrag-test/load_to_fuseki.sh` (требует запущенный Fuseki)

**Критерии успеха:**
- ✅ MS GraphRAG установлен и работает с Ollama (и опционально с LM Studio для ускорения)
- ✅ Индексация завершена успешно (CPU и/или Vulkan)
- ✅ Извлечены сущности, связи, communities, summaries
- ✅ Качество триплетов высокое (все ключевые сущности найдены)
- ✅ Entity resolution работает (дубликаты объединены)
- ✅ Community detection работает (найдены логичные сообщества)
- ✅ Результаты конвертированы в RDF и загружены в Apache Jena Fuseki (п. 2.8, 2.9)

**Вопросы для оценки:**
- Качество извлечения: все ли сущности найдены?
- Entity Resolution: объединены ли дубликаты ("Alice Smith" = "Alice")?
- Связи: извлечены ли связи (Alice → worksFor → ACME)?
- Community Detection: сколько сообществ? Логичны ли они?
- Summaries: есть ли описания сообществ? Полезны ли для Schema Induction?
- Производительность: сколько времени заняла индексация 2 документов?
- Формат выхода: можем ли конвертировать в RDF для MillenniumDB?

**Время выполнения:** 3-5 дней

---

### 🔹 ФАЗА 3: LlamaIndex (Schema Induction)

**Цель:** Установить LlamaIndex, настроить для работы с **LM Studio** (OpenAI-совместимый API), протестировать извлечение онтологии (Schema Induction) из триплетов MS GraphRAG. В перспективе — отказ от Ollama в WSL2 для экономии места; один хост LLM (LM Studio на Windows).

**Связанные задачи из TASKS.md:**
- Задача **6.9** — Реализовать скрипт Schema Induction (LLM + communities)
- Задача **6.10** — Запустить Schema Induction на тестовых данных
- Задача **6.11** — Сохранить извлечённую онтологию в MillenniumDB
- Задача **6.12** — Визуализировать онтологию в WebProtégé

**Действия:**

**✅ 3.1. Установка LlamaIndex (в тот же venv)** — выполнено
```bash
source ~/projects/ferag/venv-graphrag/bin/activate
pip install llama-index llama-index-llms-ollama
```
- Установлено: llama-index 0.14.14, llama-index-llms-ollama 0.9.1, llama-index-llms-openai (для LM Studio) и зависимости.

**Перед 3.2 и 3.3 (когда понадобится LLM):** включить **LM Studio** на Windows: загрузить модель Llama 3.3 70B, включить Local Server, опция **Serve on Local Network** — чтобы WSL2 видел `http://10.7.0.3:1234`. Модель можно выгружать (Eject), когда инференс не нужен.

**✅ 3.2. Тест LlamaIndex с LM Studio** — выполнено
- Скрипт `graphrag-test/test_llamaindex.py`: клиент `openai` с `base_url="http://10.7.0.3:1234/v1"`, модель `llama-3.3-70b-instruct`, запрос через `chat.completions.create`. Ответ LM Studio получен (~66 с на один запрос).
- Запуск: `python test_llamaindex.py` (при запущенном LM Studio и загруженной модели).
- Для 3.3 (Schema Induction) используется тот же endpoint; при необходимости — обёртка через LlamaIndex или прямой вызов openai.

**✅ 3.3. Тест Schema Induction (концептуальный)** — реализован
- Скрипт `graphrag-test/test_schema_induction.py`:
  - Загружает `relationships.parquet`, `communities.parquet`, `community_reports.parquet`
  - Формирует промпт: триплеты (source → target: description) и сообщества (title, level, summary); просит извлечь OWL в Turtle: классы, object properties, rdfs:subClassOf, domain/range
  - Вызывает LM Studio (openai client, тот же endpoint), max_tokens=4096, temperature=0
  - Извлекает блок Turtle из ответа (в т.ч. из markdown), сохраняет в `extracted_ontology.ttl` и копию `extracted_ontology.owl`
- Запуск: `python test_schema_induction.py` (LM Studio должен быть запущен; выполнение может занять 10–20 мин). Для **полной** онтологии: все триплеты и отчёты, max_tokens=8192, таймаут HTTP 1 ч; результат — `extracted_ontology_full.ttl`/`.owl`, замер — `schema_induction_timing.json` (на тестовом объёме ~11 мин).

**✅ 3.4. Загрузка извлечённой онтологии в Apache Jena Fuseki** — выполнено
- Dataset `ferag-ontology` создан (TDB2). Загружена полная онтология `extracted_ontology_full.ttl` (89 триплетов, Content-Type: text/turtle).
- Проверка: http://localhost:3030 → Dataset: ferag-ontology → Query. Классы: Person, Organization, Company, Event, GEO, City, Country, University, Project, System, Technology, Team.
- Повторная загрузка: `curl -X POST 'http://localhost:3030/ferag-ontology/data' -u admin:ferag2026 -H 'Content-Type: text/turtle' --data-binary @extracted_ontology_full.ttl`

**3.5. Визуализация в WebProtégé (опционально)**
- Зарегистрироваться на https://webprotege.stanford.edu/
- Создать новый проект
- Загрузить `extracted_ontology.owl`
- Изучить структуру классов, свойств, иерархий

**Критерии успеха:**
- ✅ LlamaIndex установлен и работает с LM Studio (OpenAI-совместимый API)
- ✅ Schema Induction выполнен успешно
- ✅ LLM извлёк классы (Person, Organization и т.д.)
- ✅ LLM создал иерархию (rdfs:subClassOf)
- ✅ LLM определил domain/range для свойств
- ✅ Извлечённая онтология логична и корректна
- ✅ Онтология сохранена в Apache Jena Fuseki (dataset: ferag-ontology)
- ✅ **RDFS inference работает:** rdfs:subClassOf автоматически выводит иерархию классов

**Вопросы для оценки:**
- Может ли LLM извлечь классы на основе communities?
- Может ли LLM создать иерархию классов?
- Может ли LLM определить domain/range для свойств?
- Качество онтологии: логична ли извлечённая схема?
- Формат выхода: можем ли сохранить в Turtle/RDF-XML?
- Производительность: сколько времени заняло извлечение?

**Время выполнения:** 2-3 дня

---

### ✅ ФАЗА 4: Интеграция и синтез (завершена: 4.1–4.6)

**Цель:** Провести второй полный цикл (новые тексты → GraphRAG → онтология), затем слить «старую» и «новую» онтологии и оба массива триплетов в результирующие онтологию и массив без задвоения и с разрешёнными коллизиями. В случае успеха — сделать вывод, что рабочий процесс вчерне готов; далее — улучшение и наращивание возможностей.

**Роли данных к началу фазы:**
- **Цикл 1 (уже выполнен):** тексты 1–3 → GraphRAG → RDF в `ferag-staging`, онтология в `ferag-ontology`.
- **Цикл 2 (в рамках фазы 4):** новые тексты → тот же пайплайн → второй staging и вторая онтология.

**Связанные задачи из TASKS.md:**
- Задача **6.13** — Реализовать сравнение/слияние онтологий (SPARQL + inference)
- Задача **6.14** — Классификация триплетов (ламинарные/турбулентные) при слиянии
- Задача **6.15** — Очистка аномалий (Graph Validation)
- Задача **6.16** — Синтез интегрированной онтологии
- Задача **6.17** — Обновить production DB (ferag-prod)

**Действия:**

**✅ 4.1. Подготовка второго корпуса**
- Подготовить новые тестовые тексты на **схожую тему** (те же домены: персоны, организации, проекты, технологии), чтобы были пересечения и возможные коллизии с первым корпусом.
- Разместить их в отдельной папке (например `graphrag-test/input-cycle2/` или второй экземпляр пайплайна) для явного разделения циклов.
- Второй корпус составлен так, чтобы при слиянии (4.3–4.4) потребовались **добавление** новых элементов онтологии и триплетов, **модификация** уже существующих и **удаление/устаревание** части старых (см. `graphrag-test/input-cycle2/README.md`).
- **Выполнено:** три документа в `graphrag-test/input-cycle2/` (cycle2_doc1–3.txt), README с перечнем ожидаемых операций при слиянии (добавление/изменение/удаление).

**✅ 4.2. Второй полный цикл (как в Фазах 2–3)**
- Запустить MS GraphRAG на новом корпусе (отдельный output/cache или копия конфигурации).
- Конвертировать результат в RDF, загрузить во **второй** staging (например dataset `ferag-staging-cycle2` или отдельный именованный граф в существующем staging).
- Выполнить Schema Induction по второму корпусу → **вторая онтология** (сохранить в dataset `ferag-ontology-cycle2` или отдельный граф).
- Итог: две онтологии (старая = ferag-ontology, новая = из цикла 2) и два массива триплетов (ferag-staging, staging цикла 2).
- **Выполнено:** корпус в `graphrag-test-cycle2/` (input + settings + prompts). Индексация GraphRAG завершена; конвертация в RDF → `graphrag_output_cycle2.ttl` (320 триплетов). Загрузка в Fuseki: для данных используется **PUT** к `.../data` (POST на данной конфигурации возвращает 405). Скрипт `graphrag-test/load_to_fuseki_cycle2.sh` — загрузка в произвольный датасет (по умолчанию ferag-staging-cycle2). Schema Induction по циклу 2 выполнена (~21 мин, LM Studio) → `extracted_ontology_cycle2.ttl` (86 триплетов онтологии); онтология загружена в датасет `ferag-ontology-cycle2`. Итог: ferag-staging (301), ferag-staging-cycle2 (320), ferag-ontology (89), ferag-ontology-cycle2 (86).

**✅ 4.3. Слияние онтологий**
- Сравнение старой и новой онтологий (SPARQL + inference): новые классы/свойства, rdfs:subClassOf/subPropertyOf, owl:equivalentClass.
- Синтез **результирующей онтологии**: объединение классов и свойств, иерархия, разрешение конфликтов и эквивалентностей (задачи 6.13, 6.16).
- Результат: одна интегрированная онтология (схема), на основе которой будет выполняться слияние триплетов.
- **Выполнено:** скрипт `graphrag-test/merge_ontologies.py` (rdflib): загрузка обеих онтологий, объединение графов (дубликаты триплетов не добавляются). Результирующая онтология: `graphrag-test/integrated_ontology.ttl` (161 триплет). Отчёт сравнения: `graphrag-test/merge_ontology_report.txt`. Классы только в цикле 1: System, Team, Technology; только в цикле 2: Consortium, Program, Workshop; общие — 9. Объектные свойства объединены (23 только из цикла 1, 22 только из цикла 2). Иерархия rdfs:subClassOf сохранена в объединённом графе. Для 4.4 и 4.5 использовать `integrated_ontology.ttl`.

**✅ 4.4. Слияние массивов триплетов**
- На основе **результирующей онтологии**: привести оба набора триплетов к единой схеме (типы сущностей, типы связей).
- **Дедупликация:** устранить задвоение триплетов (одинаковые субъект–предикат–объект).
- **Разрешение коллизий:** противоречия (один и тот же субъект/объект с разными предикатами или разными типами) — по правилам или пометка для ручной проверки (задачи 6.14, 6.15).
- Результат: **результирующий массив триплетов**, соответствующий результирующей онтологии, без дубликатов и с учётом разрешённых коллизий.
- **Выполнено:** скрипт `graphrag-test/merge_triples.py`: загрузка `graphrag_output.ttl` (301) и `graphrag_output_cycle2.ttl` (320). Дедупликация по (s, p, o). Коллизии (s, ferag:description, o): оставлено описание из цикла 2. Связи (Relationship) с одинаковыми from, to, description объединены. Результат: `graphrag-test/integrated_triples.ttl` (595 триплетов). Отчёт: `merge_triples_report.txt`. Добавлено из цикла 2: 272 триплета, пропущено точных дубликатов: 48.

**✅ 4.5. Обновление production**
- Apache Jena Fuseki dataset `ferag-prod`: загрузить **результирующую онтологию** и **результирующий массив триплетов** (задача 6.17).
- Включить OWL/RDFS reasoning для автоматического вывода.
- **Выполнено:** скрипт `graphrag-test/load_to_prod.sh`: объединение `integrated_ontology.ttl` (161) и `integrated_triples.ttl` (595) в один граф → `prod_merged.ttl` (756 триплетов); создан датасет `ferag-prod`, загрузка через PUT. Проверка: SPARQL COUNT возвращает 756. Reasoning: при необходимости включается в настройках датасета Fuseki (конфигурация сервера).

**✅ 4.6. Проверка полного цикла**
- SPARQL-проверки: классы и свойства на месте, триплеты корректны, иерархия и inference работают.
- **Критерий успеха фазы:** два цикла выполнены, результирующая онтология и результирующий массив триплетов получены. **Готовность:** цикл построения графа (Тезис → Антитезис → Синтез → production) вчерне готов; **готовность рабочего процесса в целом** — после появления возможности диалога с LLM по сохранённым данным (RAG/диалог). Далее: минимальные шаги к диалогу, затем — улучшение и наращивание (проекции, Protege, LLM-ревизия слияния).
- **Выполнено:** скрипт `graphrag-test/verify_prod.py` — SPARQL-запросы к ferag-prod. Результаты: 756 триплетов; 15 классов (owl:Class); 45 объектных свойств; 11 связей rdfs:subClassOf; сущности и связи присутствуют. Отчёт: `verify_prod_report.txt`. **Вывод: цикл построения графа вчерне готов; для оценки рабочего процесса в целом необходимо реализовать диалог с LLM (RAG).**

**Критерии успеха:**
- ✅ Второй корпус подготовлен и второй цикл (GraphRAG → RDF → Schema Induction) выполнен
- ✅ Две онтологии и два массива триплетов явно зафиксированы (ferag-ontology / ferag-ontology-cycle2, ferag-staging / ferag-staging-cycle2)
- ✅ Слияние онтологий выполнено (результирующая онтология: integrated_ontology.ttl, 161 триплет)
- ✅ Слияние триплетов выполнено (integrated_triples.ttl, 595 триплетов; дедупликация и коллизии по описаниям)
- ✅ Production (ferag-prod) обновлён: результирующая онтология + результирующий массив триплетов (756 триплетов); reasoning — при необходимости в конфигурации Fuseki
- ✅ Полный цикл построения графа проверен; цикл вчерне готов; готовность рабочего процесса в целом — после реализации RAG/диалога

**Время выполнения:** 3–5 дней

**Перспектива (подлежит реализации позже): привлечение LLM к слиянию**

Слияние онтологий (4.3) и слияние массивов триплетов (4.4) — ключевой интеллектуальный момент процесса; сейчас они выполняются формальными процедурами (объединение графов, правила по коллизиям). В перспективе предусмотреть **LLM-ревизию**:

- **Гибрид:** правила остаются первой линией; LLM вызывается для коллизий (один субъект — противоречащие факты), кандидатов на смысловое дублирование, неочевидных соответствий классов/свойств между онтологиями.
- **Контекст для LLM:** спорный триплет/сущность + выдержки из **исходных текстов** корпуса, где они упоминаются; при необходимости — запрос к **интернету** по открытым фактам; при сомнении — **обращение к пользователю** (варианты решения + краткое обоснование).
- **Результат:** более осмысленная дедупликация, обоснованное разрешение коллизий, аудит решений; воспроизводимость базового пайплайна сохраняется, LLM-ревизия — опциональная стадия поверх него.

См. задачу 6.23 в [TASKS.md](../tasks/TASKS.md) и [PROJECT-003.md](../project/PROJECT-003.md) (LLM-ревизия).

---

### 🔹 ФАЗА 5: PostgreSQL + Apache AGE + pgvector (отложено)

**Цель:** Установить PostgreSQL + Apache AGE + pgvector для этапов 4-5 (Проекции + RAG).

**Связанные задачи из TASKS.md:**
- Задача **4.4** — Установить PostgreSQL + Apache AGE + pgvector
- Задача **4.5** — Подключиться к PostgreSQL, проверить расширения AGE и pgvector
- Задача **4.6** — Создать первый граф в Apache AGE
- Задача **4.7** — Изучить основы Cypher в Apache AGE
- Задача **4.8** — Создать тестовые узлы и рёбра (проверка работы AGE)
- Задача **4.9** — Протестировать pgvector: создать таблицу с эмбеддингами, векторный поиск

**Статус:** ⏸️ **ОТЛОЖЕНО** до момента, когда этапы 1-3 (MillenniumDB + MS GraphRAG + Schema Induction) работают стабильно.

**Обоснование:** PostgreSQL нужен только на этапе "Проекции" (этап 4), когда RDF-данные из MillenniumDB нужно синхронизировать в Property Graph (Apache AGE) для быстрых Cypher-запросов и RAG. Сначала нужно стабилизировать основной цикл (1-3).

**Действия (когда понадобится):**

**5.1. Установка VectorGraph (PostgreSQL + AGE + pgvector)**
```bash
cd ~/projects
git clone https://github.com/QuixiAI/vectorgraph.git
cd vectorgraph
docker-compose up -d
```

**5.2. Проверка расширений**
```sql
psql -h localhost -U postgres
CREATE EXTENSION IF NOT EXISTS age;
CREATE EXTENSION IF NOT EXISTS vector;
LOAD 'age';
SET search_path = ag_catalog, "$user", public;
```

**5.3. Тест Apache AGE (Cypher)**
```sql
SELECT create_graph('test_graph');
SELECT * FROM cypher('test_graph', $$
  CREATE (a:Person {name: 'Alice'})-[:KNOWS]->(b:Person {name: 'Bob'})
  RETURN a, b
$$) as (a agtype, b agtype);
```

**5.4. Тест pgvector**
```sql
CREATE TABLE documents (
  id SERIAL PRIMARY KEY,
  content TEXT,
  embedding vector(384)
);
INSERT INTO documents (content, embedding) VALUES (...);
SELECT content FROM documents ORDER BY embedding <=> '[...]' LIMIT 5;
```

**Время выполнения (когда понадобится):** 1-2 дня

---

## 📊 Сводная таблица: Фазы и задачи

| Фаза | Компонент | Задачи из TASKS.md | Время | Приоритет |
|------|-----------|-------------------|-------|-----------|
| 1 | **MillenniumDB** | 4.10 | 1-2 дня | 🔥 Высокий |
| 2 | **MS GraphRAG** | 6.3, 6.4, 6.5, 6.6, 6.7, 6.8 | 3-5 дней | 🔥 Высокий |
| 3 | **LlamaIndex** | 6.9, 6.10, 6.11, 6.12 | 2-3 дня | 🔥 Высокий |
| 4 | **Интеграция** | 6.13, 6.14, 6.15, 6.16, 6.17 | 2-3 дня | 🔥 Высокий |
| 5 | **PostgreSQL+AGE+pgvector** | 4.4, 4.5, 4.6, 4.7, 4.8, 4.9 | 1-2 дня | ⏸️ Средний (отложено) |

**Общее время (Фазы 1-4):** 8-13 дней  
**Общее время (с Фазой 5):** 9-15 дней

---

## 🎯 Критерии успеха проекта

### Минимальный успех (MVP)

- ✅ MillenniumDB установлен и работает (SPARQL endpoint)
- ✅ MS GraphRAG установлен и работает с Ollama
- ✅ MS GraphRAG индексирует тестовый корпус (2-5 документов)
- ✅ Триплеты загружаются в MillenniumDB
- ✅ LlamaIndex извлекает онтологию из триплетов (Schema Induction)
- ✅ Онтология сохраняется в MillenniumDB

### Полный успех

- ✅ Всё из "Минимального успеха"
- ✅ Полный цикл работает: Тезис → Антитезис → Синтез
- ✅ Сравнение онтологий работает (новая vs существующая)
- ✅ Классификация триплетов работает (ламинарные/турбулентные)
- ✅ Очистка аномалий работает
- ✅ Синтез онтологий работает
- ✅ Production DB обновляется корректно

### Расширенный успех (с PostgreSQL)

- ✅ Всё из "Полного успеха"
- ✅ PostgreSQL + Apache AGE + pgvector установлены
- ✅ Синхронизация RDF → Property Graph работает
- ✅ Cypher-запросы работают (Apache AGE)
- ✅ Векторный поиск работает (pgvector)

---

## 📝 Заметки

### Архитектурные решения

1. **MS GraphRAG и LlamaIndex:** Нативная установка (venv) для фазы тестирования, контейнеризация отложена до момента стабилизации.

2. **MillenniumDB:** Контейнер с самого начала (легко портировать на Fedora).

3. **PostgreSQL:** Отложен до момента, когда этапы 1-3 работают стабильно.

4. **Тестовый корпус:** 2-5 документов (~1-2 МБ текста) для начала, потом масштабировать.

5. **Портативность:** Вся структура проекта в `~/projects/ferag`, при переезде на Fedora достаточно `git clone` + повторить установку компонентов.

### Риски и митигация

| Риск | Вероятность | Митигация |
|------|------------|-----------|
| MS GraphRAG не работает с Ollama | Средняя | Проверить настройки API, использовать альтернативные модели |
| MillenniumDB не поддерживает нужные SPARQL-функции | Низкая | Изучить документацию, рассмотреть альтернативы (Apache Jena Fuseki) |
| Schema Induction даёт низкое качество онтологии | Средняя | Улучшить промпты, использовать более мощную модель (70B → 120B) |
| Индексация MS GraphRAG слишком медленная | Высокая | **Смягчено:** LM Studio + Vulkan (AMD 780M) даёт ~40% ускорение; опционально — меньшая модель или Fedora/ROCm |
| Недостаточно ОЗУ для Llama 3.3 70B | Низкая | Использовать Q4_K_M квантование (40 ГБ), закрыть другие приложения |

---

## 🔗 Ссылки

- **Документация проекта:** [docs/project/PROJECT.md](../project/PROJECT.md)
- **Задачи:** [docs/tasks/TASKS.md](../tasks/TASKS.md)
- **Журнал:** [docs/journal/JOURNAL.md](../journal/JOURNAL.md)
- **Транскрипт чата:** [26-0201_chat.txt](../../26-0201_chat.txt)

---

## 📌 Текущий статус исполнения (актуализировано 13.02.2026)

| Фаза | Статус | Примечание |
|------|--------|------------|
| **1** (Apache Jena Fuseki) | ✅ Выполнена | Dataset ferag-test, RDFS inference проверен |
| **2** (MS GraphRAG) | ✅ Выполнена | 2.1–2.9 и 2.7a; RDF в Fuseki (ferag-staging) |
| **3** (LlamaIndex) | ✅ Выполнена | 3.1–3.4; Schema Induction, онтология в ferag-ontology |
| **4** (Интеграция) | ✅ Выполнена | 4.1–4.6: второй корпус, слияние онтологий и триплетов, ferag-prod (756 триплетов), проверка |
| **5** (PostgreSQL+AGE+pgvector) | ⏸️ Отложена | После выхода на RAG/диалог — по необходимости |

**Готовность:** Цикл построения графа (Фазы 1–4) вчерне готов. Рабочий процесс в целом считается готовым после появления возможности диалога с LLM по сохранённым данным (RAG/диалог).

---

## ✅ Следующие шаги (после данного плана)

1. **Достичь возможности диалога с LLM по графу (RAG/диалог)** — минимальный набор шагов без обязательного развёртывания Фазы 5: запрос к ferag-prod (SPARQL), формирование контекста, вызов LLM, ответ пользователю. См. план продолжения: `docs/chats/26-0213-1049_plan.md`.
2. После работающего диалога — по необходимости: Фаза 5 (проекции, pgvector), LLM-ревизия слияния, масштабирование.

**Примечание.** MillenniumDB в плане не развёрнут; в качестве RDF-хранилища используется Apache Jena Fuseki. Резюме работы по плану: `docs/chats/26-0210-1500_plan_resume.md`.
